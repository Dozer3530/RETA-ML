# RETA ML — core dependencies (pure Python/GeoPandas; no QGIS)
pandas>=1.3.0
numpy>=1.20.0
pyproj>=3.0.0
geopandas>=0.10.0
scikit-learn>=0.24.0
scipy>=1.7.0
pytest>=6.0.0
# Optional: for Parquet I/O
pyarrow>=6.0.0
# Sprint 3: graph construction and PyG Dataset
torch>=2.0.0
torch_geometric>=2.4.0

# Sprint 6: hyperparameter optimisation
optuna

# Tabular baseline (LOFO benchmark)
xgboost

# GUI app + visualization (scripts/benchmark.py, scripts/interactive_map.py, app.py)
streamlit
plotly
matplotlib

# DOCX report export (LOFO v2/v4 reports + app download button)
python-docx
